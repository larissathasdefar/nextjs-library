import Typography from "@mui/material/Typography";
// TODO: reuse form from edit to create user
export default function CreateUser() {
  return (
    <div>
      <Typography variant="h4" sx={{ marginBottom: "40px" }}>
        Create user
      </Typography>
    </div>
  );
}
